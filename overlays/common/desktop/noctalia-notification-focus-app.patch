--- a/src/shell/notification/notification_toast.cpp
+++ b/src/shell/notification/notification_toast.cpp
@@ -2289,7 +2289,21 @@
     if (data.button == BTN_RIGHT) {
       requestClose(id, CloseReason::Dismissed);
-    } else if (data.button == BTN_LEFT && hasDefaultAction) {
-      if (m_notifications != nullptr) {
+    } else if (data.button == BTN_LEFT) {
+      // nix-config: focus the originating app via the wlr foreign-toplevel
+      // protocol (same path as the dock), so the click jumps to the app
+      // regardless of xdg-activation / Hyprland focus_on_activate.
+      if (m_notifications != nullptr && m_wayland != nullptr) {
+        for (const auto& n : m_notifications->all()) {
+          if (n.id == id) {
+            const std::string appId = n.desktopEntry.value_or(n.appName);
+            if (!appId.empty()) {
+              m_wayland->activateToplevelForAppId(appId);
+            }
+            break;
+          }
+        }
+      }
+      if (hasDefaultAction && m_notifications != nullptr) {
         const std::string activationToken =
             m_wayland != nullptr ? m_wayland->requestActivationToken(sourceSurface) : std::string{};
         if (!m_notifications->invokeAction(id, "default", activationToken, true)) {
